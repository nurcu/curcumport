# Curcumport
Command Line importer for curcudb.

`curcush -v`

## Import from CSV

Usage:
`curcush -i <file> -f <format>`

## Import from API

Usage:
`curcush -u <uri> -a <address> -k <secret> -p <password>`
